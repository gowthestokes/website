---
import SocialLinks from './SocialLinks.astro';

interface Props {
  showSocials?: boolean;
}

const { showSocials = true } = Astro.props;
---

<footer>
  <p>© {new Date().getFullYear()} David Dlima</p>
  {showSocials && <SocialLinks />}
</footer>
